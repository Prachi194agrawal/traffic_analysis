# Local development defaults to SQLite when DATABASE_URL is omitted.
DATABASE_URL=sqlite:///./traffic_analysis.db
CORS_ORIGINS=http://localhost:5173
MAX_UPLOAD_BYTES=15728640

# Only required for private Hugging Face model repositories.
HF_TOKEN=replace_with_your_huggingface_token
